{% extends 'mzInventarioBundle::layout.html.twig' %}
{% import 'mzInventarioBundle::macros.html.twig' as macros %}

{% block title %} {{ parent() }} - Préstamos {% endblock %}

{% block pagetitle %} Agregar Préstamo {% endblock %}

{% block page %}
    <div class="btn-toolbar">
        {{ macros.btn_volver('prestamo') }}
    </div>
    <form class="well" action="{{ path('prestamo_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>{{ macros.btn_guardar() }}</p>
    </form>
{% endblock %}
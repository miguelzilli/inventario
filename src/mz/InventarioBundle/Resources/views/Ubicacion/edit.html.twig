{% extends 'mzInventarioBundle::layout.html.twig' %}
{% import 'mzInventarioBundle::macros.html.twig' as macros %}

{% block title %} {{ parent() }} - Ubicaciones {% endblock %}

{% block pagetitle %} Editar Ubicación {% endblock %}

{% block page %}
    <div class="btn-toolbar">
        {{ macros.btn_volver('ubicacion') }}
        {{ macros.btn_borrarf('ubicacion_delete', entity.id, delete_form) }}
    </div>
    <form class="well" action="{{ path('ubicacion_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        {{ form_widget(edit_form) }}
        <p>{{ macros.btn_guardar('ubicacion') }}</p>
    </form>
{% endblock %}
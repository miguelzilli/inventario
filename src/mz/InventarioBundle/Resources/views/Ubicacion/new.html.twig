{% extends 'mzInventarioBundle::layout.html.twig' %}
{% import 'mzInventarioBundle::macros.html.twig' as macros %}

{% block title %} {{ parent() }} - Ubicaciones {% endblock %}

{% block pagetitle %} Agregar Ubicación {% endblock %}

{% block page %}
    <div class="btn-toolbar">
        {{ macros.btn_volver('ubicacion') }}
    </div>
    <form class="well" action="{{ path('ubicacion_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>{{ macros.btn_guardar() }}</p>
    </form>
{% endblock %}
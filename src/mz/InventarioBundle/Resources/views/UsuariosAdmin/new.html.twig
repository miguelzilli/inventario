{% extends 'mzInventarioBundle::layout.html.twig' %}
{% import 'mzInventarioBundle::macros.html.twig' as macros %}

{% block title %} {{ parent() }} - Usuarios {% endblock %}

{% block pagetitle %} Agregar Usuario {% endblock %}

{% block page %}
    <div class="btn-toolbar">
        {{ macros.btn_volver('usuario') }}
    </div>
    <form class="well" action="{{ path('usuario_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>{{ macros.btn_guardar() }}</p>
    </form>
{% endblock %}